// すべてのスライドとフォームを削除し、jsonObjの中身も初期化する
function removeAll(){
	$('#slide').children().remove();
	$('#questions').children().remove();
	$('#comments').children().remove();
	jsonObj = {
		slide:[],
		questions:[],
		comments:[],
	};
	socket.emit('removeAll_from_client',jsonObj);
}